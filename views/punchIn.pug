html
	head
		title= title 
	body
		div(style = {"padding":"20px"})
			
			form#formSubmit
				label(for="idField") Id :
				input(
  				type='text'
  				name='id',value="" id="inputId" placeholder="Id")
				button(type="submit" id="formId") punch In
		script.
			const formDataElement = document.getElementById('formSubmit')
			formDataElement.addEventListener('submit',preventDefaultActionFun)
			function preventDefaultActionFun(event){
				event.preventDefault()
			}
			const submitElement = document.getElementById('formId')
			submitE1.addEventListener('click',myFormFunction)
			//function for punch in and out
			function myFormFunction(event){
				submitElement.textContent = "punch Out"
				//User enter Id 
				const idValue = inputId.value
				console.log(idValue)
			}
			


				
